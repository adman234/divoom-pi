"""divoom-pi: a Raspberry Pi Bluetooth-Classic-to-TCP gateway for Divoom devices.

Speaks the same TCP wire protocol as the ESP32 `divoom-gateway` firmware, so
Home Assistant's `divoom` custom integration talks to a Raspberry Pi exactly
like it talks to an ESP32 - no changes on the Home Assistant side.
"""

__version__ = "1.0.0"
